@model IEnumerable<NVHN_0461.Models.Wallet>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Money)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameWallet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Money)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameWallet)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WalletId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WalletId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WalletId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
